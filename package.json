{
  "name": "camera-booking",
  "version": "1.0.0",
  "description": "Full-stack Camera Booking Website with React, Express, and MongoDB",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "npm run frontend",
    "dev:all": "concurrently -n backend,frontend -c blue,green \"npm run backend\" \"npm run frontend\"",
    "backend": "npm run dev --prefix backend",
    "seed": "npm run seed --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
